//Objeto Math

let resulsato;

resulsato = Math.PI;
resulsato = Math.round(2.4);
resulsato = Math.ceil(2.4); //Ceil siempre redondea para arriba
resulsato = Math.floor(2.4); //floor redondea para abajo
resulsato = Math.sqrt(144);
resulsato = Math.abs(-200);
resulsato = Math.min(3, 5, 6, 8, 9);
resulsato = Math.max(3, 5, 6, 8, 9);
resulsato = Math.random();
resulsato = Math.floor(Math.random() * 30);


console.log(resulsato);